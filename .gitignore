node_modules/
dist/
build/
.turbo/
coverage/
*.tsbuildinfo

.env
.env.*
!.env.example

packages/database/generated/

# Os cores de emulação são baixados no setup — grandes e reproduzíveis.
apps/web/public/emulator/
# apps/web/public/roms/ NÃO entra aqui de propósito: o homebrew do catálogo é
# versionado para que o arquivo e o SHA-256 do seed andem no mesmo commit.
# Ver docs/homebrew.md.
playwright-report/
test-results/

.DS_Store
*.log

# Worktrees dos agentes
.claude/worktrees/
